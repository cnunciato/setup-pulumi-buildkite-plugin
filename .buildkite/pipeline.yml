steps:
  - label: ":linux: Linux hosted agent test"
    command: |
      echo $$PATH
      
    agents:
      queue: hosted-linux
    plugins:
      - cnunciato/setup-pulumi#$BUILDKITE_COMMIT:
          install_root: /usr/local
  
  - label: ":mac: macOS hosted agent test"
    command: |
      echo $$PATH 
      
    agents:
      queue: hosted-macos
    plugins:
      - cnunciato/setup-pulumi#$BUILDKITE_COMMIT:
          install_root: /usr/local